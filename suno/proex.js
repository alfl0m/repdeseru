/**
 * A direction on the horizontal axis.
 * @enum {number}
 */
const HorizontalDirection = {
    LEFT: -1,
    RIGHT: 1,
};

// Usage
const direction = HorizontalDirection.LEFT;
console.log(direction); // Output: -1
